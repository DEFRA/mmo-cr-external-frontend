{% extends 'layouts/page.njk' %}
{% from "govuk/components/summary-list/macro.njk" import govukSummaryList %}
{% from "govuk/components/button/macro.njk" import govukButton %}

{% block pageContent %}
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
      <h1 class="govuk-heading-xl" data-testid="app-heading-title">{{ heading }}</h1>

      {% for section in sections %}
        <h2 class="govuk-heading-l">{{ section.heading }}</h2>
        {% if section.vesselLabel %}
          <h3 class="govuk-heading-m">{{ section.vesselLabel }}</h3>
        {% endif %}
        {{ govukSummaryList({ rows: section.rows }) }}
      {% endfor %}

      {{ govukButton({
        text: "View all catch records",
        href: "/records",
        classes: "govuk-button--secondary"
      }) }}
    </div>
  </div>
{% endblock %}
