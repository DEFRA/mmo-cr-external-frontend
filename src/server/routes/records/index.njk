{% extends 'layouts/page.njk' %}
{% from "govuk/components/button/macro.njk" import govukButton %}
{% from "govuk/components/notification-banner/macro.njk" import govukNotificationBanner %}
{% from "govuk/components/tag/macro.njk" import govukTag %}
{% from "govuk/components/pagination/macro.njk" import govukPagination %}
{% from "govuk/components/details/macro.njk" import govukDetails %}

{% block pageContent %}
  {{ govukNotificationBanner({
    titleText: notice.title,
    text: notice.text
  }) }}

  {{ appHeading({
    text: heading,
    caption: accountRole
  }) }}

  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
      <table class="govuk-table" data-testid="app-records-table">
        <thead class="govuk-table__head">
          <tr class="govuk-table__row">
            <th scope="col" class="govuk-table__header">Trip end date</th>
            <th scope="col" class="govuk-table__header">Vessel</th>
            <th scope="col" class="govuk-table__header">Status</th>
            <th scope="col" class="govuk-table__header">Created by</th>
          </tr>
        </thead>
        <tbody class="govuk-table__body">
          {% for record in records %}
            <tr class="govuk-table__row" data-testid="app-records-row">
              <td class="govuk-table__cell">
                <a class="govuk-link" href="{{ record.href }}">{{ record.tripEndDate | formatDate("d MMM yyyy") }}<span class="govuk-visually-hidden"> ({{ record.statusText }} record)</span></a>
              </td>
              <td class="govuk-table__cell">{{ record.vesselName }}</td>
              <td class="govuk-table__cell">
                {{ govukTag({ text: record.statusText, classes: record.statusTagClasses }) }}
              </td>
              <td class="govuk-table__cell">{{ record.createdBy }}</td>
            </tr>
          {% endfor %}
        </tbody>
      </table>

      <p class="govuk-body" data-testid="app-records-results">
        Showing {{ pagination.firstItem }} to {{ pagination.lastItem }} of {{ pagination.total }}
      </p>

      {{ govukPagination({
        items: paginationItems
      }) }}

      {{ govukButton({
        text: "Create a new catch record",
        href: "/draft"
      }) }}

      {% call govukDetails({ summaryText: "How to record a catch" }) %}
        {% include "partials/how-to-record-a-catch.njk" %}
      {% endcall %}
    </div>
  </div>
{% endblock %}
